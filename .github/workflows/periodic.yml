name: Periodic Task

on: 
  workflow_dispatch: 
  # schedule:
  #   - cron: '*/5 * * * *'

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Check the deployed service url
        uses: jtalk/url-health-check-action@v5
        with:
          url: https://fso-pokedex-cicd.fly.dev/health